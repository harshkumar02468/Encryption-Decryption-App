# Encryption-Decryption-App
This repository contains a File Encryption/Decryption application built using Python oops and Tkinter. The app allows users to encrypt and decrypt text files with a  generated key with over one Trillion combinations providing a simple and effective GUI. The encrypted files can later be decrypted using only the same key by which it is encrypted.
